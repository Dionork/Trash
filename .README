public class JvmComprehension {

    public static void main(String[] args) {
        int i = 1;                      // 1
        Object o = new Object();        // 2
        Integer ii = 2;                 // 3
        printAll(o, i, ii);             // 4
        System.out.println("finished"); // 7
    }

    private static void printAll(Object o, int i, Integer ii) {
        Integer uselessVar = 700;                   // 5
        System.out.println(o.toString() + i + ii);  // 6
    }
}

АпликейшнКлассЛоадер берет public class JvmComprehension и загружает данные о нем в МетаСпейс
Далее запрос передается в БутсТрапКлассЛоадер и ПлатформКласслоадер
Эти загрузчики ищут в коде программы используемые классы и загружают информацию по ним.
Если все классы найдены, то программа проверяется на валидность. Проверка связанности этих классов.
В программе нет никаких статических блоков, значит идет выполнение основного тела программы с метода мейн.
Эго фрейм создается в Стеке.
Далее программа идет по строчкам указанных в комментарии.
1 - создание в стеке переменную типа инт со значением 1.
2 - В хипе создается место для new Object() и ссылку на него передает в СТек и присваевает ее переменной о.
3 - В хипе выделяется место под класс Integer в котором лежит значение 2 и ссылку передают в переменную в Стеке ii.
4 - Создание фрейма printAll.
    передача данных из переменных в метод printAll.
    Программа переходит в сам метод. Создаются новые переменные, в которые копируются значения из переданных переменных (для примитивных типов)
    или копируются ссылки объектов.
5 - В Хипе создается объект типа Integer со значением 700, затем ссылку на него передается в стек и привязывается к созданной
    переменной uselessVar.
6 - Создается фрейм для o.toString()
    Вычисляется выражение o.toString()
    Вычисляется выражение o.toString() + i + ii
    Создается фрейм для System.out.println.
    Вызов метода println у объекта out, который находится в классе System
    В этот метод передается значение выражения o.toString() + i + ii
    Вывод этого значения.
    Закрытие метода printAll
7 - Создается фрейм для System.out.println.
     Вызов метода println у объекта out, который находится в классе System.
     Передается значение finished.
     Вывод значения на экран.
     Завершение метода.
Завершение метода main. Завершение фрейма main.
Начинает работу сборщик мусора. Помечает не используемые объекты без ссылок. Удаляет помеченные объекты.